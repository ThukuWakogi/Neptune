<UserControl
    x:Class="Neptune.Controls.MaterialCategotyTemplateControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Neptune.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">

    <Grid Margin="10" >
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="300"/>
        </Grid.ColumnDefinitions>
        <TextBlock Name="CategoryTextBox" Text="{x:Bind MaterialCategory.Category}" FontSize="30" HorizontalAlignment="Left"/>
        <TextBlock Name="NumberTextBox" Text="{x:Bind MaterialCategory.DisplayMaterialsNumber}" Grid.Row="1" FontSize="15" HorizontalAlignment="Left"/>
    </Grid>
</UserControl>
